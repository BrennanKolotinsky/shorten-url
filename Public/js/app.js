// brackets for dependencies
var app = angular.module('shortUrlApp', []); //use the shortUrlApp in the HTML

// controller is logic for a mini app
app.controller('shortAppCtrl', ($scope) => {
    // $scope.urlToShorten = "Hello World";
});